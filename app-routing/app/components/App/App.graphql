query GetApp($title: String) {
  App(model: { title: $title }) {
    title
    pages {
      name
    }
  }
}

query GetRecentArticles($title: String) {
  App(model: { title: $title }) {
    title
    pages {
      articles {
        publishDate
        headline
      }
      name
    }
  }
}

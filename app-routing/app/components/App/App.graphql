query GetApp($title: String) {
  App(model: { title: $title }) {
    title
    pages {
      name
    }
  }
}

query GetAppArticles($title: String) {
  App(model: { title: $title }) {
    pages {
      articles {
        headline
        pageName
        publishDate
      }
    }
    title
  }
}

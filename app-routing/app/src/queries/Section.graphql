query GetSection($slug: String, $id: ID) {
  Section(model: { slug: $slug, id: $id }) {
    name
    slug
    articles {
      headline
      publishDate
      slug
      id
      section {
        name
        slug
        id
      }
    }
  }
}

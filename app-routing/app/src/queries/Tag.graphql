query GetTag($slug: String, $id: ID) {
  Tag(model: { slug: $slug, id: $id }) {
    category
    articles {
      headline
      id
      slug
      section {
        name
        slug
        id
      }
    }
  }
}

query GetContentItem($id: ID) {
  PageEntry(model: { id: $id }) {
    ... on Article {
      __typename
      id
      section {
        id
      }
      tags {
        id
      }
    }
    ... on Section {
      __typename
      id
    }
  }
}

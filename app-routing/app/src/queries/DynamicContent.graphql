query GetContentItem($id: ID) {
  PageEntry(model: { id: $id }) {
    ... on Article {
      __typename
      id
    }
    ... on Section {
      __typename
      id
    }
    ... on Tag {
      __typename
      id
    }
  }
}

query GetAllTagsAndArticles {
  Tags {
    tags {
      category
      id
      slug
    }
  }
  Articles {
    articles {
      slug
      headline
      id
      tags {
        category
      }
      section {
        name
        slug
        id
      }
    }
  }
}

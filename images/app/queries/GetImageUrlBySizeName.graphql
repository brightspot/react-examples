query GetImageUrlBySizeName($id: ID, $name: String) {
  Image(id: $id) {
    imageFile {
      size(name: $name) {
        src
        height
        width
        name
        srcSets {
          size
          src
        }
      }
    }
    title
  }
}

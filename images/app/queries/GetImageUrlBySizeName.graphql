query GetImageUrlBySizeName($name: String) {
  Images {
    items {
      title
      imageFile {
        size(name: $name) {
          height
          width
          name
          src
        }
        contentType
        filename
      }
    }
  }
}
